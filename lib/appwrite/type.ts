export interface userInterface {
    title: string, slug: string, content: string, featuredImage: string, status: boolean, userId: string
}
export interface User {
    name?: string;
    email: string;
    password: string
}